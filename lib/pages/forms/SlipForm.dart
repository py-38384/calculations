import "package:flutter/material.dart";

class SlipForm extends StatelessWidget {
  const SlipForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Calculation Form');
  }
}